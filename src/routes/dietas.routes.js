const { Router } = require("express");
const controller = require("../controllers/dietas.controller");
const authMiddleware = require("../middlewares/auth.middleware");
const { auditAction } = require("../middlewares/audit.middleware");
const { authorizeRoles } = require("../middlewares/role.middleware");
const router = Router();
router.use(authMiddleware);
router.get("/", authorizeRoles("ADMIN", "OPERADOR"), controller.list);
router.get("/:id", authorizeRoles("ADMIN", "OPERADOR"), controller.getById);
router.post("/", authorizeRoles("ADMIN", "OPERADOR"), auditAction("DIETAS", "CREAR"), controller.create);
router.put("/:id", authorizeRoles("ADMIN", "OPERADOR"), auditAction("DIETAS", "EDITAR"), controller.update);
router.delete("/:id", authorizeRoles("ADMIN"), auditAction("DIETAS", "ELIMINAR"), controller.remove);
module.exports = router;
